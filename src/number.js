const _ = require('lodash');

function tostr(n) {
  console.log(_tostr(n));
}

function _tostr(n, max, min, len) {
  const e = String(n.toExponential()).split('e');
  const v = e[0].replace('.', '');
  const l = v.replace('-', '').length;
  const ex = Number(e[1]);

  const N = len - min;
  const M = BigInt(2 + _.repeat('0', N + max));
  const V = BigInt(v + _.repeat('0', N - l + ex));

  // const N = -min;
  // const M = BigInt(2 + _.repeat('0', N + max));
  // const V = BigInt(v + _.repeat('0', N + ex));

  return M + V;
}

// function _tostr(n, max, min, len) {
//   const e = String(n.toExponential()).split('e');
//   const v = e[0].replace('.', '');
//   const l = v.replace('-', '').length;
//   const ex = Number(e[1]);

//   const N = len - min;
//   const M = BigInt(2 + _.repeat('0', N + max));
//   const V = BigInt(v + _.repeat('0', N - l + ex));

//   // const N = -min;
//   // const M = BigInt(2 + _.repeat('0', N + max));
//   // const V = BigInt(v + _.repeat('0', N + ex));

//   return M + V;
// }

const data = [
  // 3323456789120000000000000000,
  // 332345678900000000000000000,
  // 33234567890000000000000000,
  // 3323456789000000000000000,
  // 332345678900000000000000,
  // 33234567890000000000000,
  // 3323456789000000000000,
  // 332345678900000000000,
  // 33234567890000000000,
  // 3323456789000000000,
  // 332345678900000000,
  // 33234567890000000,
  // 3323456789000000,
  // 332345678900000,
  // 33234567890000,
  // 3323456789000,
  // 332345678900,
  // 33234567890,
  // 3323456789,
  // 332345678.9,
  // 33234567.89,
  // 3323456.789,
  332345.6789,
  33234.56789,
  3323.456789,
  332.3456789,
  33.23456789,
  3.323456789,
  0.3323456789,
  0.03323456789,
  0.003323456789,
  // 0.0003323456789,
  // 0.00003323456789,
  // 0.000003323456789,
  // 0.0000003323456789,
  // 0.00000003323456789,
  // 0.000000003323456789,
  // 0.0000000003323456789,
  // 0.00000000003323456789,
  // -0.00000000003323456789,
  // -0.0000000003323456789,
  // -0.000000003323456789,
  // -0.00000003323456789,
  // -0.0000003323456789,
  // -0.000003323456789,
  // -0.00003323456789,
  // -0.0003323456789,
  // -0.003323456789,
  // -0.03323456789,
  // -0.3323456789,
  // -3.323456789,
  // -33.23456789,
  // -332.3456789,
  // -3323.456789,
  // -33234.56789,
  // -332345.6789,
  // -3323456.789,
  // -33234567.89,
  // -332345678.9,
  // -3323456789,
  // -33234567890,
  // -332345678900,
  // -3323456789000,
  // -33234567890000,
  // -332345678900000,
  // -3323456789000000,
  // -33234567890000000,
  // -332345678900000000,
  // -3323456789000000000,
  // -33234567890000000000,
  // -332345678900000000000,
  // -3323456789000000000000,
  // -33234567890000000000000,
  // -332345678900000000000000,
  // -3323456789000000000000000,
  // -33234567890000000000000000,
  // -332345678900000000000000000,
];

// 199999966765432110000000000000000000000000000000000000000000000n

function main() {
  let max = -100;
  let min = +100;
  let len = 0;
  _.each(data, (n) => {
    const e = String(n.toExponential()).split('e');
    const ex = Number(e[1]);
    max = Math.max(max, ex);
    min = Math.min(min, ex);
    len = Math.max(len, e[0].length);
  });

  _.each(data, (n) => {
    console.log(_tostr(n, max, min, len));
  });
  console.log(max, min, len);
}

main();
